# Пример настроек для ip@janado.de (ispgateway.de)
# Проверьте настройки SMTP в Thunderbird -> Outgoing Server (SMTP)
# Обычно для ispgateway.de:
# SMTP_HOST=sslmailpool.ispgateway.de (или smtp.ispgateway.de)
# SMTP_PORT=587 (TLS) или 465 (SSL)
# SMTP_SECURE=false (для 587) или true (для 465)
